# Hostinger environment variables (names only)
# Hostinger provides PORT automatically.

NODE_ENV=production
# Optional: used by some generators/routes in the full app, not required for the prebuilt server
BASE_URL=https://alarmbeepguide.com
